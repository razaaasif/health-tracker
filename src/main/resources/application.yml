spring:
  application:
    name : health-tracker
  datasource:
    url: jdbc:postgresql://localhost:5432/health_tracker
    username: postgres
    password: postgres
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    schemas: health_tracker
  jpa:
    hibernate:
      ddl-auto: validate
